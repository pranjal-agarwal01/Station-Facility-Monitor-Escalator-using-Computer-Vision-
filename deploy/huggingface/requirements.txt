# Installed on top of the Gradio SDK image. CPU wheels keep the Space small.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics>=8.3
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0
imageio-ffmpeg>=0.5
matplotlib>=3.7
